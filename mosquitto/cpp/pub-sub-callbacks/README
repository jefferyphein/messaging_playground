Run "make" to build both an async publisher and async subscriber.

The subscriber subscribes to a pre-defined topic, and waits until it receives
a "shutdown" message before it disconnects and exits.

The publisher publishes a few messages, followed by a "shutdown" message,
disconnects, and exits.
