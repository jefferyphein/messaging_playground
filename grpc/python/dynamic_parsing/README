Run "make" to build necessary descriptors and data blobs.

Run "make ssh" to test SSH functionality.

Run "make sftp" to test SFTP functionality.

Run "make file" to test FILE functionality.

Run "make etcd" to test ETCD functionality.
